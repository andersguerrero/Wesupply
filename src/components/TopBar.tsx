export default function TopBar() {
  return (
    <div
      className="border-b border-white/10 bg-[var(--brand-neutral)] py-2 text-center text-[12px] font-medium uppercase tracking-wider text-white/80"
      style={{ fontFamily: "var(--font-subheading)" }}
    >
      Envíos a todo Argentina · Descuentos por volumen
    </div>
  );
}
