/**
 * Shopify Storefront API client.
 * Se implementará cuando SHOPIFY_STORE_DOMAIN y SHOPIFY_STOREFRONT_ACCESS_TOKEN estén configurados.
 *
 * Uso futuro:
 *   const client = createStorefrontClient({ ... });
 *   const data = await client.request(QUERY);
 */
export const NOT_CONFIGURED = true;
